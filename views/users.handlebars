<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registros</title>
</head>
<body>
    <header>
      <nav>
        <div class="logo">
          <img src="/img/servico-de-gestao.png" alt="">
          <h1>Ordem de Serviço</h1>
        </div>

        <div class="mobile-menu">
          <div class="line1"></div>
          <div class="line2"></div>
          <div class="line3"></div>
        </div>
        <ul class="nav-list">
          <li><button class="button-nav" onclick="ModalUser()">Cadastrar Usuário</button></li>
          <li><a class="button-nav" href="/">Serviços</a></li>
          <li><a class="button-nav" href="/users">Usuários</a></li>
          <li><a class="button-nav" href="/login/logout">Sair</a></li>
        </ul>
      </nav>
    </header>

    <div class="modal" id="modalUser">
        <div class="modal-content">
            <header class="modal-header">
                <img src="/img/caduser.png" class="modal-img" alt="">
                <h2>Novo Usuário</h2>
            </header>
            <form action="/users" method="POST" class="modal-form">
                <input class="modal-field" placeholder="Login" type="text" name="login"required/> 
                <input class="modal-field" placeholder="Senha" type="password" name="senha" required/>
                <input class="modal-field" placeholder="Confirmação de Senha" type="password" name="confirm"required/>

                <div class="caduser-boxinput">
                    <label class="modal-label" for="admin">Admin</label>
                    <input class="modal-checkbox" value="true" name="admin" id="admin" type="checkbox">
                </div>
                    
                <div class="modal-buttons">
                    <button class="button green" type="submit">Enviar</button>
                    <button class="button green" id="caduserClose" onclick="ModalUser()" type="button">Fechar</button>
                </div>
            </form>
        </div>
    </div>

    <div class="registros-group">
        <input class="registros-search" id="searchbar" placeholder="Buscar..."/>
    </div>

    <div class="users-mat">
        <table id="tabela" class="table-users table">
            <thead>
                <tr>
                    <th>Admin</th>
                    <th class="table-th-nome">Nome de Usuário</th>
                    <th colspan="2">Opções</th>
                </tr>
            </thead>
            
            <tbody>
                {{#each posts}}
                    <tr>
                        {{#if admin}}
                        <td>
                            Sim
                        </td>
                        {{else}}
                        <td>
                            Não
                        </td>
                        {{/if}}
                        <td>{{login}}</td>
                        <td class="td-button">
                            <a href="/users/permissao/{{id}}"><button class="registros-button">Alterar</button></a> 
                        </td>
                        <td class="td-button">
                            <a href="/users/finalizar/{{id}}"><button class="registros-button">Encerrar</button></a>
                        </td>
                    </tr>			
                {{/each}}
            </tbody>
        </table>                
    </div>
    <script type="text/javascript" src="http://code.jquery.com/jquery-1.7.2.min.js"></script>
    <script src="/js/main.js"></script>  
</body>
</html>




